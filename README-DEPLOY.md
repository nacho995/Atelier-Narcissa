# Deploy Instructions for Vercel

## Main Website (Root Directory)
- Framework: Vite
- Root Directory: (leave empty or use ".")
- Build Command: npm run build
- Output Directory: dist
- Install Command: npm install

## Important Files
- Main website is in the ROOT directory
- Panel is in /panel/frontend/ (separate deployment if needed)
- Server is in /server/ (separate deployment if needed)

The main website uses:
- React + Vite
- Tailwind CSS
- Framer Motion for animations
